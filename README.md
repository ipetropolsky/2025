# Сервис "Итоги года"

Одностраничный веб-сервис для подведения личных итогов года с возможностью поделиться результатами.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```
Приложение будет доступно по адресу: [http://localhost:5173](http://localhost:5173) (или другому порту, если 5173 занят)

### Сборка для production
```bash
npm run build
```
Собранные файлы будут в папке `dist/`

## 🧪 Тестирование

### Тесты логики приложения

В проекте есть несколько тестовых файлов для проверки логики:

1. **Тест декодирования данных** - проверка работы с base64 и URL-encoding:
```bash
node test-data.js
```

2. **Тест логики отображения** - проверка правильности отображения вопросов и ответов:
```bash
node test-logic.js
```

3. **Тест URL encoding** - проверка корректности обработки данных в URL:
```bash
node test-url-encoding.js
```

4. **Тест функции decodeBase64** - проверка работы улучшенной функции декодирования:
```bash
node test-decode.js
```

### Тесты в браузере

Также есть HTML-файлы для тестирования в браузере:

1. **`test-browser.html`** - комплексный тест декодирования и логики отображения
2. **`test-atob.html`** - тест работы функции `atob` с реальными данными

Откройте эти файлы прямо в браузере для визуальной проверки.

## 🔗 Формат данных

Данные сохраняются в двух местах:
1. **`localStorage`** - ключ `yearReview2025`
2. **URL параметр** - `?data=<base64>`

### Структура данных
```typescript
{
  questions: Array<{
    id: number;
    text: string;
    type: 'single' | 'multiple';
    maxAnswers?: number;
  }>;
  answers: Record<string, string[]>; // ключ - ID вопроса
  custom: Array<{
    question: string;
    answer: string[];
  }>;
}
```

### Base64 encoding
Используется URL-safe base64:
- `+` заменяется на `-`
- `/` заменяется на `_`
- `=` (padding) удаляется

## 🐛 Отладка

### Распространенные проблемы

1. **Ошибка "Failed to execute 'atob'**:
   - Проверьте, что данные в URL корректно закодированы
   - Используйте `test-decode.js` для проверки

2. **Не отображаются результаты по ссылке**:
   - Убедитесь, что в данных есть поле `questions`
   - Проверьте консоль браузера на наличие ошибок

3. **Некорректное отображение кириллицы**:
   - Функции `encodeBase64`/`decodeBase64` используют TextEncoder/TextDecoder для корректной работы с Unicode

### Консоль браузера
Для отладки откройте DevTools (F12) и проверьте:
- Ошибки в консоли
- Сетевые запросы
- Состояние localStorage

## 📁 Структура проекта

```
src/
  App.tsx              # Основной компонент приложения
  main.tsx             # Точка входа
  vite.config.ts       # Конфигурация Vite
public/                # Статические файлы
dist/                  # Собранные файлы (генерируется)
test-*.js              # Тестовые скрипты
test-*.html            # HTML тесты
```

## 🚀 Деплой на GitHub Pages

Приложение настроено для деплоя на GitHub Pages по пути `https://<username>.github.io/2025/`

### Шаги для деплоя:

1. **Сборка проекта:**
```bash
npm run build
```

2. **Деплой на GitHub Pages:**
```bash
npm run deploy
```

Эта команда:
- Соберет проект в папку `dist/`
- Создаст ветку `gh-pages` (если ее нет)
- Запушит содержимое папки `dist/` в ветку `gh-pages`

3. **Настройка GitHub Pages:**
   - Перейдите в репозиторий на GitHub
   - Settings → Pages
   - Source: выберите **Branch: gh-pages**
   - Folder: оставьте **/(root)**
   - Нажмите Save

4. **Доступ к приложению:**
   - После деплоя приложение будет доступно по адресу: `https://<username>.github.io/2025/`
   - Обычно публикация занимает 1-2 минуты

### Локальная проверка перед деплоем:
```bash
npm run build
npm run preview
```
Приложение будет доступно на `http://localhost:4173` для проверки собранной версии.

### Важные замечания:
- Base path установлен как `/2025/` в `vite.config.ts`
- Все ссылки в приложении автоматически учитывают этот путь
- При локальной разработке (`npm run dev`) base path игнорируется

## 📄 Лицензия

MIT
